# Twilio Notification - Chrome Extension

A chrome extension that interfaces with the Twilio dashboard and makes a sound when a new task arrives.
Also dispatches requests to a Web Push provider server.
